name: Nexus Intelligence Pipeline

on:
  schedule:
    - cron: "0 * * * *"   # Runs every hour
  workflow_dispatch:

jobs:
  run-intelligence:
    runs-on: ubuntu-latest

    permissions:
      contents: write

    steps:

      # 1️⃣ Checkout Repository
      - name: Checkout Repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 1

      # 2️⃣ Setup Python 3.12
      - name: Setup Python
        uses: actions/setup-python@v4
        with:
          python-version: "3.12"
          cache: "pip"

      # 3️⃣ Upgrade pip and install dependencies
      - name: Install Dependencies
        run: |
          python -m pip install --upgrade pip
          pip install --prefer-binary -r requirements.txt

      # 4️⃣ Cache SQLite databases (optional, speeds up repeated runs)
      - name: Cache Databases
        uses: actions/cache@v3
        with:
          path: |
            ./db/*.db
          key: nexus-db-${{ runner.os }}-${{ hashFiles('db/*.db') }}
          restore-keys: |
            nexus-db-${{ runner.os }}-

      - name: Run Nexus Engine
        run: |
          echo "🚀 Starting Nexus Engine at $(date)"
          python nexus_core.py
          python range_engine.py
          python hybrid_engine.py
          python ai_engine.py
          python run_consensus.py
          python nexus_dispatcher.py
