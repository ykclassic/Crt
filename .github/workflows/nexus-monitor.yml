name: Nexus Alert Monitor

on:
  workflow_run:
    workflows: ["Nexus Intelligence Pipeline"]
    types:
      - completed
  workflow_dispatch:

jobs:
  run-monitor:
    runs-on: ubuntu-latest
    # Only run if the previous workflow actually succeeded
    if: ${{ github.event.workflow_run.conclusion == 'success' || github.event_name == 'workflow_dispatch' }}

    env:
      PYTHONUNBUFFERED: 1
      WEBHOOK_URL: ${{ secrets.WEBHOOK_URL }}
      SINGLE_RUN: "true"

    steps:
      # 1️⃣ Checkout
      - name: Checkout Repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 1

      # 2️⃣ Download Database Artifact across workflow boundaries
      - name: Download Signals Database
        uses: dawidd6/action-download-artifact@v7
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          workflow: nexus_pipeline.yml # Make sure this matches your primary workflow filename exactly
          run_id: ${{ github.event.workflow_run.id }}
          name: nexus_signals_db
          path: .

      # 3️⃣ Confirm DB Presence
      - name: Verify Database
        run: |
          if [ ! -f nexus_signals.db ]; then
            echo "❌ Database not found after artifact download."
            ls -R
            exit 1
          fi
          echo "✅ Database loaded."

      # 4️⃣ Setup Python
      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"
          cache: pip

      # 5️⃣ Install Dependencies
      - name: Install Dependencies
        run: |
          python -m pip install --upgrade pip
          pip install --prefer-binary -r requirements.txt
          pip install --upgrade ccxt

      # 6️⃣ Run Alert Monitor
      - name: Run Alert Monitor
        run: |
          echo "🛡 Running Alert Monitor at $(date)"
          python alert_monitor.py
